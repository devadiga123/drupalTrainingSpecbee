"use strict";
const main = module.exports.main = {};

main['isverified'] = (options, event, context, callback) => {
    if(context.simpledb.roomleveldata.phone.verified){
        options.next_state = "verified";
    }else{
        options.next_state = "notverified";
    }
    callback(options, event, context)
}